{
  "name": "eidos-crm-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E suite for the EIDOS CRM (runs against a staging deployment)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
